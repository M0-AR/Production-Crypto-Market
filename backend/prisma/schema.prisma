generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Stateless app: no sessions in Postgres. Cache lives in Redis; jobs in BullMQ/Redis.
// (The prototype CachedMarket table was dropped — dead tables are not sellable.)
